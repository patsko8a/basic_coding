Формат ввода
Вводится одно натуральное число.

Формат вывода
Требуется составить математическое выражение — произведение простых неубывающих чисел, которое в результате даёт исходное.
ПРИМЕЧАНИЕ: код корректно работает в колабе, но чекер яндекса выдаёт ошибку:

stdout:
/bin/sh ./build.sh 1>&2


stderr:
solution.py:7:11: E111 indentation is not a multiple of 4
Код не соответствует стандарту PEP8
или в нем есть синтаксические ошибки
make: *** [Makefile:2: build] Error 1




N = int(input())

for i in range(2, int(N ** 0.5) + 2):
    while N % i == 0:
        print(i, end='')
        if (N / i) != 1:
          print(' * ', end='')
        N = N / i
